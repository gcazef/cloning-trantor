/*
** EPITECH PROJECT, 2018
** trantor_cmd
** File description:
** Functions for command reception
*/

#include <string.h>
#include "trantor_elements.h"

int check_cmd(char *client_message)
{
    /*int i = 0;
    
    char *comp = strdup(client_message);

    strtok(comp, " ");
    while (i < 8){
        if (strcmp(command[i], comp) == 0)
            return (1);
        else
            i++;
    }*/
    client_message = (void *)client_message;
    return (1);
}